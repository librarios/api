# librarios
A server that manages ebook files


## Build

```bash
# build & run
$ make build && make run

# build release executable
$ make build-release

# build documents and open in the browser
$ make doc
```
